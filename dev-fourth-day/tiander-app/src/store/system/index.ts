import actions from "./actions";
import reducer from "./reducer";
import * as types from "./types";

export {
  actions as systemInfoActions,
  reducer as systemInfoReducer,
  types as systemInfoTypes,
};
